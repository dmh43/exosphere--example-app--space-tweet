extends ../layout

block content
  h2 #{count} Users

  table
    tr
      th NAME
      th PASSWORD
      th
    each user in users
      tr
        td= user.name
        td= user.password
        td


  a(href="/users/new") add a user
