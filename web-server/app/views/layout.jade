doctype html
html
  head
    title SpaceTweet!
    script(type="text/javascript" src="/assets/main.js")
  body
    nav
      if user
        form(method=routes.destroySession().method, action=routes.destroySession())
          button.logout Logout
    h1 SpaceTweet
    block content
